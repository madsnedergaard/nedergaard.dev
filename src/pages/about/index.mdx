import { Post } from '../../components';

export const meta = {
  title: 'About things',
  summary: 'A summary of this note',
  createdAt: '', // only for personal knowledge
  updatedAt: 'Aug 04, 2020', // TODO: Replace with "now"
  type: 'page', // note
  topics: ['meta', 'real knowledge'],
  // tags/topics
  // stage: '', // evergreen, seedling, budding
};

export default ({ children }) => <Post meta={meta}>{children}</Post>;

# About something

This is a text about something...
