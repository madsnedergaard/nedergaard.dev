import { Post } from '../../components';

export const meta = {
  title: 'About me',
  description: 'Some descriptipn',
  date: 'Aug 04, 2020', // TODO: Replace with "now"
  type: 'page', // note
  // tags/topics
  // stage: '', // evergreen, seedling, budding
};

export default ({ children }) => <Post meta={meta}>{children}</Post>;

# About

A test MDX page.
